# Record ID detail URLs

Activity opportunity detail pages will use the Feishu `record_id` as the stable identifier, with an optional readable slug for display and SEO. The application resolves details by `record_id`, so title changes or duplicate campaign names do not break existing links.
